<p>We're not around, but we'd love to chat another time.</p>
<%= form_for @contact, :url => { :action => :create }, remote: true do |f| %>
<%= f.text_field :name, :required => true, :placeholder => 'Your Name' %>
<%= f.email_field :email, :required => true, :placeholder => 'Your Email' %>
<%= f.text_area :message, :as => :text, :required => true, :placeholder => 'We still really want to hear from you! Leave us a note:' %>
<div class="hold">
	<a href="" id="btn-close">Close X</a>
	<%= f.submit 'Send' %>
</div>
<% end %>
